[project]
name = "mts-dialog"
version = "0.1.0"
requires-python = ">=3.9
dependencies = [
  "pandas",
  "scikit-learn",
  "transformers",
  "datasets",
  "evaluate"
]

[tool.setuptools.packages.find]
where = ["src"]