# Note pacienti chiropractor

Am generat cate o fisa Markdown pentru fiecare conversatie transcrisa folosibila din `data/chiropractor_ro/audio`.

Surse folosite:
- `audio12`-`audio17`: am folosit versiunile mai curate din `data/chiropractor_ro/transcribe_examples/transcribe12.txt` pana la `transcribe17.txt`.
- `audio18`-`audio35` si `audio_1`-`audio_4`: am folosit fisierele `.txt` din `data/chiropractor_ro/audio`.

Nu am creat fise separate pentru `data/chiropractor_ro/audio/transcribe12.txt` pana la `transcribe17.txt`, deoarece dubleaza conversatiile `audio12`-`audio17`.

Conventii:
- fiecare camp are procent de incredere intre paranteze patrate;
- informatia lipsa este marcata `not specified`;
- informatia dedusa sau instabila este marcata `unsure`.
